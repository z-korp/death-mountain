runtime: nodejs20

handlers:
  - url: /(.*\..+)$
    static_files: dist/\1
    upload: dist/.*\..+$

  - url: /.*
    static_files: dist/maintenance.html
    upload: dist/maintenance.html
    secure: always

env_variables:
  GENERATE_SOURCEMAP: false
  NODE_ENV: 'production'
